//
//  ContentView.swift
//  SwiftMatrix3x3
//
//  Created by Omkar Ranawade on 2/17/26.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        HStack{
            Text("Swift UI")
                .frame(width: 100, height: 100)
                .foregroundColor(.red)
            Text("Swift UI")
                .frame(width: 100, height: 100)
                .foregroundColor(.red)
            Text("Swift UI")
                .frame(width: 100, height: 100)
                .foregroundColor(.red)
        }
        HStack{
            Text("Swift UI")
                .frame(width: 100, height: 100)
                .foregroundColor(.red)
            Text("Swift UI")
                .frame(width: 100, height: 100)
                .foregroundColor(.red)
            Text("Swift UI")
                .frame(width: 100, height: 100)
                .foregroundColor(.red)
        }
        HStack{
            Text("Swift UI")
                .frame(width: 100, height: 100)
                .foregroundColor(.red)
            Text("Swift UI")
                .frame(width: 100, height: 100)
                .foregroundColor(.red)
            Text("Swift UI")
                .frame(width: 100, height: 100)
                .foregroundColor(.red)
        }
    }
}

#Preview {
    ContentView()
}
